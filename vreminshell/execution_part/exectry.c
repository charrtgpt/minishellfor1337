#include <unistd.h>
#include <stdio.h>
#include <stdlib.h>
 #include <sys/types.h>
#include <sys/wait.h>

int main(int ac, char **av, char **env)
{
    // int save;
    // int ksave;
    // printf("how many times\n");
    // int a = fork();
    // if (a == 0)
    // {
    //     printf("\nhelllllo\n\n");
    // }
    // int i = waitpid(a, &save, 0);
    // if(a == 0)
    //     printf("\nat the child %d\n", i);
    // if(a != 0)
    //     printf("\nat the parent %d\n", i);
    // // int b = fork();
    // // if (b == 0)
    // // {
    // //     printf("\nsecond process\n\n");
    // // }
    // // waitpid(b, &ksave, 0);
    // if(a != 0)
    // {
    //     printf("\nparent pro\n");
    //     printf("\nat the parent %d\n", a);
    // }
    // char **rt = malloc(3234);
    // char **tester;
    // tester[0] = "./a.out";
    // tester[1] = NULL;
    // execve(tester[0], tester, env);
    printf("errrrrrrror\n");
    // _exit(1);
    //printf("paaaar\n");
}
